<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.noegul.android.noeguluniv">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".course.CourseResultActivity"></activity>
        <activity android:name=".course.compareweights.CompareWeightsActivity" />
        <activity android:name=".course.remembercards.RememberCardsActivity" />
        <activity android:name=".course.countblocks.CountBlocksActivity" />
        <activity android:name=".course.matchthepicture.MatchThePictureActivity" />
        <activity android:name=".course.comparecoins.CompareCoinsActivity" />
        <activity android:name=".SelectCourseActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>